# إصلاح وتطوير وظيفة تعديل المنتجات

## ✅ ما تم إنجازه

### 1. إضافة API Endpoints
تم إضافة endpoints جديدة في `src/constants/API_URLS.js`:
- `PRODUCT_UPDATE(id)` - لتحديث منتج موجود
- `PRODUCT_CREATE` - لإنشاء منتج جديد
- `PRODUCT_DELETE(id)` - لحذف منتج

### 2. إضافة دوال API
تم إضافة الدوال التالية في `src/api/productsApi.js`:
- `updateProduct(productId, productData, token)` - تحديث منتج
- `createProduct(productData, token)` - إنشاء منتج جديد
- `deleteProduct(productId, token)` - حذف منتج

**ملاحظة مهمة:** جميع هذه الدوال تتطلب `token` للمصادقة.

### 3. إضافة زر التعديل في ProductDetailsScreen
- تم إضافة زر تعديل يظهر فقط للمسؤولين (admin)
- الزر يظهر بجانب زر المشاركة في الـ header
- عند الضغط عليه، يتم الانتقال إلى شاشة التعديل

### 4. إنشاء شاشة EditProductScreen
تم إنشاء شاشة جديدة `src/screens/ProductsScreen/EditProductScreen.js` تحتوي على:
- حقول إدخال لتعديل:
  - اسم المنتج (Product Name)
  - السعر (Price)
  - الفئة (Category) - مع أزرار اختيار
  - الوصف (Description)
  - العلامات (Tags)
- زر تحديث (Update Product)
- معالجة الأخطاء والتحقق من البيانات
- رسائل نجاح/خطأ

### 5. ربط مع Navigation
تم إضافة `EditProductScreen` إلى `ProductsStack` في `AppNavigator_NEW.js`

## 🔧 كيفية الاستخدام

### للمستخدمين (المسؤولين فقط):
1. افتح صفحة تفاصيل المنتج
2. إذا كنت مسؤولاً (admin)، سيظهر زر التعديل (✏️) في الـ header
3. اضغط على زر التعديل
4. عدّل البيانات المطلوبة
5. اضغط على "Update Product"
6. سيتم تحديث المنتج في الباك اند

### للمطورين:

#### استخدام دالة updateProduct:
```javascript
import { updateProduct } from '../api/productsApi';
import { useSelector } from 'react-redux';

const token = useSelector((state) => state.auth.token);

const handleUpdate = async () => {
  try {
    const productData = {
      name: 'New Product Name',
      description: 'Product description',
      price: 29.99,
      category: 'raw_ingredients',
      tags: ['gluten-free', 'organic']
    };
    
    const result = await updateProduct(productId, productData, token);
    console.log('Product updated:', result);
  } catch (error) {
    console.error('Error:', error);
  }
};
```

## 🔐 الأمان

- جميع عمليات التعديل تتطلب token للمصادقة
- يتم التحقق من صلاحيات المستخدم (admin فقط)
- يتم التحقق من صحة البيانات قبل الإرسال

## 📡 ربط الباك اند

### Endpoint المطلوب في الباك اند:
```
PUT /api/products/:id
```

### Headers المطلوبة:
```
Content-Type: application/json
Authorization: Bearer <token>
```

### Body المطلوب:
```json
{
  "name": "Product Name",
  "description": "Product description",
  "price": 29.99,
  "category": "raw_ingredients",
  "tags": ["gluten-free", "organic"]
}
```

### Response المتوقع:
```json
{
  "success": true,
  "data": {
    "_id": "product_id",
    "name": "Product Name",
    "description": "Product description",
    "price": 29.99,
    "category": "raw_ingredients",
    "tags": ["gluten-free", "organic"],
    ...
  }
}
```

## ⚠️ ملاحظات مهمة

1. **التحقق من صلاحيات المستخدم:**
   - الزر يظهر فقط إذا كان `user.role === 'admin'` أو `user.isAdmin === true`
   - تأكد من أن الباك اند يرسل هذه المعلومات في بيانات المستخدم

2. **Token:**
   - تأكد من أن token موجود في Redux store
   - Token يتم إرساله تلقائياً في headers

3. **معالجة الأخطاء:**
   - يتم عرض رسائل خطأ واضحة للمستخدم
   - يتم تسجيل الأخطاء في console للتتبع

4. **التحديث التلقائي:**
   - بعد التحديث الناجح، يتم العودة لصفحة التفاصيل
   - قد تحتاج إلى إعادة تحميل البيانات يدوياً

## 🐛 استكشاف الأخطاء

### الزر لا يظهر:
- تحقق من أن المستخدم مسجل دخول
- تحقق من أن `user.role === 'admin'` أو `user.isAdmin === true`
- تحقق من console للأخطاء

### خطأ في التحديث:
- تحقق من أن الباك اند يعمل
- تحقق من أن token صحيح
- تحقق من console للأخطاء التفصيلية
- تأكد من أن endpoint `/api/products/:id` موجود في الباك اند

### البيانات لا تُحدث:
- تحقق من response من الباك اند
- تحقق من أن البيانات المرسلة صحيحة
- تحقق من console logs

## 📝 الملفات المعدلة/المضافة

1. ✅ `src/constants/API_URLS.js` - إضافة endpoints
2. ✅ `src/api/productsApi.js` - إضافة دوال API
3. ✅ `src/screens/ProductsScreen/ProductDetailsScreen.js` - إضافة زر التعديل
4. ✅ `src/screens/ProductsScreen/EditProductScreen.js` - شاشة جديدة
5. ✅ `src/components/navigation/AppNavigator_NEW.js` - إضافة الشاشة للـ navigation

## ✨ الميزات المستقبلية (اختيارية)

- رفع صورة للمنتج
- حذف المنتج
- إنشاء منتج جديد
- معاينة التغييرات قبل الحفظ
- إلغاء التغييرات

